import pygame

# Before we can do much with pygame, we will need to initialize it. This line will attempt to 
# initialize all the pygame modules.
pygame.init()

# Window size in pixels
SCREEN_WIDTH, SCREEN_HEIGHT = 640, 640